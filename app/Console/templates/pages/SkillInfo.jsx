import React from 'react'
import MenuHeader from '../../components/Layouts/MenuHeader'

export const ${name_pascal}Info = (props) => {
  return(
    <div>
      <MenuHeader title='${name_pascal}s Information' />
    </div>  
  )
}

export default ${name_pascal}Info 